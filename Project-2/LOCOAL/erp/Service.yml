# Update your service
apiVersion: v1
kind: Service
metadata:
  name: erp-app-service
  namespace: erp-dev
spec:
  type: NodePort
  selector:
    app: erp-app
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30081
