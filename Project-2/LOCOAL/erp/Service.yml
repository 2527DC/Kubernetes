apiVersion: v1
kind: Service
metadata:
  name: erp-app-service
  namespace: erp-dev
spec:
  type: NodePort
  selector:
    app: erp-app
  ports:
    - port: 80
      targetPort: 80 # Changed from 8000 to 80
      nodePort: 30081
