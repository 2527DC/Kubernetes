apiVersion: v1
kind: Service
metadata:
  name: erp-app-service
  namespace: erp-dev
spec:
  type: ClusterIP
  selector:
    app: erp-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
